'use strict';
var app = app || {};

(function (module) {

  const mealView = {};

  app.mealView.initMealPage = function () {
    app.showOnly('#meal-view');

    $('#meal-view').empty();
    .append()
    })
  }

  module.mealView = mealView;

})(app)
